# Fancy colored prompt
export PS1="\[\e[36m\]\u@web \W> \[\e[m\]"

# Don't allow prompt mangling when virtual env is activated
export VIRTUAL_ENV_DISABLE_PROMPT=1

# VIM hotkeys in terminal <3
set -o vi
